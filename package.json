{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://bettina-sam.github.io/disaster-management",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
    /* …your other deps… */
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "shx": "^0.3.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",

    /* --- GitHub Pages helpers --- */
    "postbuild": "shx cp build/index.html build/404.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -b gh-pages"
  }
}
